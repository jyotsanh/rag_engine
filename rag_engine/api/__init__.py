"""FastAPI application: the operator surface (Swagger UI at ``/docs``).

The app object lives in :mod:`rag_engine.api.app`; run it with ``uv run dev`` /
``uv run server`` (see :func:`rag_engine.dev` / :func:`rag_engine.server`).
"""
